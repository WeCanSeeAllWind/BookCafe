version: '1.29.2'

services:
  api:
    build: ./api
    ports: 
      - "80:5000"
    volumes:
      - ./api:/app
    environment:
      FLASK_ENV: development
  web:
    build: ./web
    ports:
      - "3000:3000"
    volumes:
      - ./web:/usr/src/app
    depends_on:
      - api